{
  "name": "my-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:win": "electron-packager ./ --overwrite --platform=win32 --arch=x64 DescargarMusicaDeYouTube",
    "build:winInstaller": "node build_installer.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.1.2"
  },
  "dependencies": {
    "electron-context-menu": "^2.3.0",
    "electron-wix-msi": "^2.2.0",
    "path": "^0.12.7",
    "youtube-mp3-downloader": "^0.7.3"
  }
}
